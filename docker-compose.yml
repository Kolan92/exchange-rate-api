version: '3'

services:
  # exchangeRateApi:
  #   image: kolan1992/echange_rate_api
  #   restart: on-failure
  #   depends_on:
  #     - postgresql
  #   environment:
  #     - PSQL_USER=root
  #     - PSQL_PASSWORD=password
  #     - PSQL_HOST=postgresql
  #     - PSQL_PORT=5432
  #     - DATABASE-HOST=postgresql
  #     - DATABASE-SSL-MODE=disable
  #     - DATABASE-USERNAME=exchange_rate_api_user
  #     - DATABASE-PASSWORD=123
    # ports:
    #   - 8080:8080
  postgresql:
    image: postgres:14.2-alpine
    healthcheck:
      test: [ "CMD", "pg_isready", "-q", "-d", "postgres", "-U", "root" ]
      timeout: 45s
      interval: 10s
      retries: 10
    restart: always
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=password
    volumes:
      - ./scripts/db:/docker-entrypoint-initdb.d/

